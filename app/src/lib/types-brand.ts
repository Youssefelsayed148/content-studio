export interface Brand {
  id: string;
  name: string;
  slug: string;
  logoUrl: string;
  primaryColor: string;
  description: string;
  configName: string;
  createdAt: string;
  updatedAt: string;
}
